// import { history } from '../../helpers';

export const allEvent = {
    finishUploadMovie
}

function finishUploadMovie(is_finish) {
    // console.log("coucou "+is_finish)
    return dispatch => {
        console.log("<°)))><")
        // dispatch({ finish_upload_movie: is_finish })
    }
}